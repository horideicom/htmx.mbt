{
  "name": "htmx-mbt",
  "private": true,
  "scripts": {
    "build:htmx-test": "moon build --target js",
    "demo": "node cmd/demo/server.js",
    "test:htmx": "pnpm build:htmx-test && pnpm exec wtr --config web-test-runner.config.mjs --concurrency 3",
    "test:coverage": "pnpm build:htmx-test && pnpm exec wtr --config web-test-runner.config.mjs --coverage --concurrency 3",
    "coverage:view": "xdg-open coverage/index.html"
  },
  "devDependencies": {
    "@web/test-runner": "^0.20.2",
    "@web/test-runner-chrome": "^0.18.1",
    "@web/test-runner-coverage-v8": "^0.8.0",
    "chai": "^4.5.0",
    "chai-dom": "^1.12.1",
    "mocha": "^11.7.4",
    "mock-socket": "^9.3.1",
    "sinon": "^10.0.1"
  }
}